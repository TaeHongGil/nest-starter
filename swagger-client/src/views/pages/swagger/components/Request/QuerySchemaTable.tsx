import { observer } from 'mobx-react';
import { SwaggerProps } from '../../Swagger';

const QuerySchemaTable = observer(({ store }: SwaggerProps) => {
  const test = '';

  return <></>;
});

export default QuerySchemaTable;
