import { createClasses } from '@root/views/pages/management/material-kit/theme/create-classes';

// ----------------------------------------------------------------------

export const logoClasses = {
  root: createClasses('logo__root'),
};
